<h4>Welcome to Pharmeasy</h4>

<% if user_signed_in? %>
    <li>
      <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
    </li>
<% else %>
    <li>
      <%= link_to('Login', new_user_session_path)  %>
    </li>
<% end %>

<%= render "#{@current_user.users_role.name.to_s.downcase}_home"%>
